:root{--bg:#020510;--surface:#080e1f;--surface2:#0d1530;--accent:#00c8ff;--accent2:#0055ff;--accent3:#ff2d78;--gold:#f5c842;--text:#e8eaf6;--muted:#6b7a99;--card-border:rgba(0,200,255,0.15);}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{background:var(--bg);color:var(--text);font-family:'Syne',sans-serif;overflow-x:hidden}
::-webkit-scrollbar{width:4px}::-webkit-scrollbar-track{background:var(--bg)}::-webkit-scrollbar-thumb{background:var(--accent);border-radius:2px}
nav{position:sticky;top:0;z-index:200;display:flex;align-items:center;justify-content:space-between;padding:0 48px;height:72px;background:rgba(2,5,16,0.95);backdrop-filter:blur(20px);border-bottom:1px solid rgba(0,200,255,0.12)}
.logo-wrap{display:flex;align-items:center;gap:12px;text-decoration:none}
.logo-img{width:46px;height:46px;object-fit:contain;border-radius:50%}
.logo-text{font-family:'Orbitron',monospace;font-weight:900;font-size:1.25rem;letter-spacing:.15em;background:linear-gradient(135deg,#00c8ff,#0055ff);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
nav ul{display:flex;gap:24px;list-style:none}
nav ul a{color:var(--muted);text-decoration:none;font-size:.76rem;letter-spacing:.08em;text-transform:uppercase;transition:color .2s}
nav ul a:hover{color:var(--accent)}
.nav-btn{background:none;border:1px solid var(--card-border);color:var(--text);padding:7px 18px;border-radius:2px;font-family:'Syne',sans-serif;font-size:.78rem;cursor:pointer;transition:all .2s}
.nav-btn:hover{border-color:var(--accent);color:var(--accent)}
.hero{position:relative;min-height:calc(100vh - 72px);display:flex;flex-direction:column;justify-content:center;padding:80px 48px;overflow:hidden}
.hero-bg{position:absolute;inset:0;background:radial-gradient(ellipse at 70% 50%,rgba(0,85,255,.15) 0%,transparent 60%),radial-gradient(ellipse at 20% 80%,rgba(0,200,255,.08) 0%,transparent 50%);pointer-events:none}
.hero-content{position:relative;z-index:1;max-width:680px}
.hero-label{display:inline-flex;align-items:center;gap:8px;font-size:.72rem;letter-spacing:.2em;text-transform:uppercase;color:var(--accent);margin-bottom:24px}
.hero-label::before{content:'';display:block;width:32px;height:1px;background:var(--accent)}
.hero h1{font-family:'Orbitron',monospace;font-size:clamp(2rem,5vw,3.8rem);font-weight:900;line-height:1.1;margin-bottom:20px}
.hero h1 em{font-style:normal;background:linear-gradient(135deg,var(--accent),var(--accent2));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero p{color:var(--muted);font-size:.95rem;line-height:1.7;max-width:480px;margin-bottom:32px}
.hero-btns{display:flex;gap:14px;flex-wrap:wrap;margin-bottom:28px}
.btn-primary{background:linear-gradient(135deg,var(--accent),var(--accent2));color:#fff;border:none;padding:13px 28px;font-family:'Syne',sans-serif;font-size:.83rem;letter-spacing:.08em;font-weight:600;border-radius:2px;cursor:pointer;transition:all .2s;text-transform:uppercase}
.btn-primary:hover{opacity:.85;transform:translateY(-2px)}
.btn-ghost{background:none;color:var(--text);border:1px solid rgba(255,255,255,.15);padding:13px 28px;font-family:'Syne',sans-serif;font-size:.83rem;letter-spacing:.08em;border-radius:2px;cursor:pointer;transition:all .2s;text-transform:uppercase}
.btn-ghost:hover{border-color:var(--accent);color:var(--accent)}
.hero-badges{display:flex;gap:10px;flex-wrap:wrap}
.hero-badge{display:flex;align-items:center;gap:7px;background:rgba(0,200,255,.06);border:1px solid var(--card-border);border-radius:2px;padding:7px 14px;font-size:.72rem;letter-spacing:.06em;text-transform:uppercase;color:var(--muted)}
.hero-badge span{color:var(--accent)}
.marquee-wrap{overflow:hidden;border-top:1px solid rgba(255,255,255,.05);border-bottom:1px solid rgba(255,255,255,.05);padding:13px 0;background:var(--surface)}
.marquee-track{display:flex;gap:56px;animation:marquee 30s linear infinite;white-space:nowrap}
.marquee-item{font-family:'Orbitron',monospace;font-size:.66rem;letter-spacing:.2em;color:var(--muted);text-transform:uppercase;display:flex;align-items:center;gap:12px;flex-shrink:0}
.marquee-item::before{content:'✦';color:var(--accent);font-size:.5rem}
@keyframes marquee{from{transform:translateX(0)}to{transform:translateX(-50%)}}
.section-tabs{display:flex;gap:4px;padding:0 48px;background:var(--surface);border-bottom:1px solid rgba(0,200,255,.08);overflow-x:auto}
.section-tabs::-webkit-scrollbar{height:2px}
.tab-btn{background:none;border:none;border-bottom:2px solid transparent;color:var(--muted);padding:15px 18px;font-family:'Syne',sans-serif;font-size:.75rem;letter-spacing:.08em;text-transform:uppercase;cursor:pointer;transition:all .2s;white-space:nowrap}
.tab-btn:hover{color:var(--text)}
.tab-btn.active{color:var(--accent);border-bottom-color:var(--accent)}
.cat-section{padding:64px 48px}
.section-header{display:flex;align-items:flex-end;justify-content:space-between;margin-bottom:36px;padding-bottom:14px;border-bottom:1px solid rgba(0,200,255,.08)}
.section-tag{font-size:.65rem;letter-spacing:.25em;text-transform:uppercase;color:var(--accent);margin-bottom:6px}
.section-title{font-family:'Orbitron',monospace;font-size:clamp(1.1rem,2.2vw,1.7rem);font-weight:700}
.section-badge{display:inline-flex;align-items:center;gap:6px;background:rgba(0,200,255,.08);border:1px solid var(--card-border);border-radius:20px;padding:5px 14px;font-size:.66rem;color:var(--accent);letter-spacing:.08em;white-space:nowrap}
.products-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:16px}
.products-grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.products-grid-5{display:grid;grid-template-columns:repeat(5,1fr);gap:14px}
.product-card{background:var(--surface);border:1px solid var(--card-border);border-radius:4px;overflow:hidden;transition:all .28s;position:relative;display:flex;flex-direction:column}
.product-card:hover{border-color:rgba(0,200,255,.45);transform:translateY(-5px);box-shadow:0 16px 36px rgba(0,0,0,.4)}
.product-badge{position:absolute;top:10px;left:10px;z-index:2;font-size:.58rem;letter-spacing:.1em;text-transform:uppercase;padding:3px 8px;border-radius:2px;font-weight:700}
.badge-apple{background:#fff;color:#000}
.badge-used{background:var(--gold);color:#000}
.badge-new{background:var(--accent);color:#000}
.badge-hot{background:var(--accent3);color:#fff}
.badge-dmg{background:#ff8c00;color:#000}
.product-img{width:100%;height:180px;background:var(--surface2);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:7px;color:var(--muted);border-bottom:1px solid var(--card-border);cursor:zoom-in;transition:background .2s}
.product-img:hover{background:#131d35}
.product-img-icon{font-size:2.2rem;opacity:.18;pointer-events:none}
.product-img-label{font-size:.62rem;letter-spacing:.1em;text-transform:uppercase;opacity:.3;pointer-events:none}
.product-info{padding:14px;flex:1;display:flex;flex-direction:column;gap:4px}
.product-cat{font-size:.6rem;letter-spacing:.14em;text-transform:uppercase;color:var(--accent)}
.product-name{font-family:'Orbitron',monospace;font-size:.8rem;font-weight:600;line-height:1.3}
.product-storage{font-size:.68rem;color:var(--muted)}
.product-sub{font-size:.68rem;color:var(--muted);line-height:1.4;flex:1}
.color-chips{display:flex;flex-wrap:wrap;gap:4px;margin:2px 0}
.chip{font-size:.58rem;padding:3px 7px;border-radius:2px;border:1px solid var(--card-border);color:var(--muted);display:flex;align-items:center;gap:3px;line-height:1.4;cursor:pointer;transition:all .18s;background:transparent;font-family:'Syne',sans-serif}
.chip:hover{border-color:var(--accent);color:var(--text)}
.chip.selected{border-color:var(--accent);color:var(--accent);background:rgba(0,200,255,.1);font-weight:600}
.c-dot{width:7px;height:7px;border-radius:50%;flex-shrink:0;display:inline-block}
.selected-label{font-size:.62rem;color:var(--accent);min-height:14px;font-weight:600;letter-spacing:.04em}
.product-footer{display:flex;align-items:center;justify-content:space-between;margin-top:6px}
.product-price{font-family:'Orbitron',monospace;font-size:.92rem;font-weight:700;color:var(--text)}
.add-btn{background:linear-gradient(135deg,#25d366,#128c4a);border:none;border-radius:2px;cursor:pointer;color:#fff;font-size:.65rem;font-weight:700;letter-spacing:.04em;text-transform:uppercase;padding:0 10px;height:30px;transition:all .2s;flex-shrink:0;white-space:nowrap;display:flex;align-items:center;gap:5px;font-family:'Syne',sans-serif}
.add-btn:hover{opacity:.85;transform:translateY(-1px)}
.add-btn svg{width:13px;height:13px;fill:#fff;flex-shrink:0}
.sub-header{display:flex;align-items:center;gap:10px;margin:32px 0 18px}
.sub-header-line{flex:1;height:1px;background:rgba(0,200,255,.1)}
.sub-header-text{font-family:'Orbitron',monospace;font-size:.74rem;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);white-space:nowrap}
.alert-bar{background:rgba(255,193,7,.07);border:1px solid rgba(255,193,7,.2);border-radius:4px;padding:11px 18px;margin-bottom:22px;font-size:.78rem;color:#ffc107;line-height:1.6;display:flex;align-items:flex-start;gap:9px}
.brands-row{display:flex;align-items:center;justify-content:space-around;padding:24px 0;border-top:1px solid rgba(255,255,255,.05);border-bottom:1px solid rgba(255,255,255,.05);gap:20px;flex-wrap:wrap}
.brand-item{font-family:'Orbitron',monospace;font-size:.85rem;font-weight:700;letter-spacing:.1em;color:var(--muted);opacity:.35;transition:all .2s}
.brand-item:hover{opacity:1;color:var(--text)}
.features-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}
.feature-card{padding:24px 20px;border:1px solid var(--card-border);border-radius:4px;background:var(--surface)}
.feature-icon{font-size:1.7rem;margin-bottom:12px;display:block}
.feature-title{font-family:'Orbitron',monospace;font-size:.76rem;font-weight:600;margin-bottom:7px}
.feature-desc{font-size:.74rem;color:var(--muted);line-height:1.6}
.newsletter{background:var(--surface);border-top:1px solid rgba(0,200,255,.08);border-bottom:1px solid rgba(0,200,255,.08);padding:56px 48px;display:flex;align-items:center;justify-content:space-between;gap:40px}
.newsletter-text .section-title{font-size:1.4rem}
.newsletter-text p{color:var(--muted);margin-top:8px;font-size:.85rem}
.newsletter-form{display:flex;flex:0 0 380px}
.newsletter-input{flex:1;background:var(--bg);border:1px solid rgba(0,200,255,.2);border-right:none;color:var(--text);padding:12px 16px;font-family:'Syne',sans-serif;font-size:.84rem;outline:none;border-radius:2px 0 0 2px;transition:border-color .2s}
.newsletter-input:focus{border-color:var(--accent)}
.newsletter-input::placeholder{color:var(--muted)}
.newsletter-btn{background:linear-gradient(135deg,var(--accent),var(--accent2));border:none;color:#fff;padding:12px 22px;font-family:'Syne',sans-serif;font-size:.78rem;font-weight:600;letter-spacing:.08em;text-transform:uppercase;cursor:pointer;border-radius:0 2px 2px 0;transition:opacity .2s}
.newsletter-btn:hover{opacity:.85}
footer{padding:56px 48px 24px;background:var(--surface);border-top:1px solid rgba(255,255,255,.04)}
.footer-grid{display:grid;grid-template-columns:2fr 1fr 1fr 1fr;gap:36px;margin-bottom:40px}
.footer-brand p{color:var(--muted);font-size:.8rem;line-height:1.7;margin-top:12px;max-width:250px}
.footer-col h4{font-family:'Orbitron',monospace;font-size:.68rem;letter-spacing:.15em;text-transform:uppercase;margin-bottom:16px}
.footer-col ul{list-style:none;display:flex;flex-direction:column;gap:9px}
.footer-col ul a{color:var(--muted);text-decoration:none;font-size:.8rem;transition:color .2s}
.footer-col ul a:hover{color:var(--accent)}
.footer-bottom{display:flex;align-items:center;justify-content:space-between;padding-top:20px;border-top:1px solid rgba(255,255,255,.05);font-size:.72rem;color:var(--muted)}
.social-links{display:flex;gap:10px}
.social-link{width:30px;height:30px;border:1px solid rgba(255,255,255,.1);border-radius:2px;display:flex;align-items:center;justify-content:center;color:var(--muted);text-decoration:none;font-size:.78rem;transition:all .2s}
.social-link:hover{border-color:var(--accent);color:var(--accent)}
/* LIGHTBOX */
.lightbox-overlay{display:none;position:fixed;inset:0;z-index:9999;background:rgba(0,0,0,.85);backdrop-filter:blur(8px);align-items:center;justify-content:center}
.lightbox-overlay.open{display:flex;animation:lbFade .2s ease}
@keyframes lbFade{from{opacity:0}to{opacity:1}}
.lightbox-box{position:relative;background:var(--surface);border:1px solid rgba(0,200,255,.25);border-radius:8px;padding:32px;max-width:460px;width:90%;display:flex;flex-direction:column;align-items:center;gap:16px;animation:lbPop .22s cubic-bezier(.34,1.56,.64,1);box-shadow:0 0 80px rgba(0,200,255,.15)}
@keyframes lbPop{from{opacity:0;transform:scale(.88)}to{opacity:1;transform:scale(1)}}
.lightbox-img-area{width:100%;aspect-ratio:1/1;max-height:300px;background:var(--surface2);border-radius:6px;border:1px dashed rgba(0,200,255,.25);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:var(--muted);overflow:hidden}
.lightbox-img-area img{width:100%;height:100%;object-fit:contain;border-radius:6px}
.lightbox-icon{font-size:4rem;opacity:.18}
.lightbox-placeholder{font-size:.72rem;letter-spacing:.1em;text-transform:uppercase;opacity:.35}
.lightbox-name{font-family:'Orbitron',monospace;font-size:1rem;font-weight:700;text-align:center;color:var(--text)}
.lightbox-sub{font-size:.78rem;color:var(--muted);text-align:center;line-height:1.6}
.lightbox-price{font-family:'Orbitron',monospace;font-size:1.3rem;font-weight:900;color:var(--accent)}
.lightbox-close{position:absolute;top:12px;right:14px;background:none;border:none;color:var(--muted);font-size:1.4rem;cursor:pointer;line-height:1;transition:color .2s;padding:4px}
.lightbox-close:hover{color:var(--text)}
@keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
.hero-content>*{animation:fadeUp .7s ease both}
.hero-content>*:nth-child(1){animation-delay:.1s}.hero-content>*:nth-child(2){animation-delay:.2s}.hero-content>*:nth-child(3){animation-delay:.3s}.hero-content>*:nth-child(4){animation-delay:.4s}.hero-content>*:nth-child(5){animation-delay:.5s}
@media(max-width:1200px){.products-grid{grid-template-columns:repeat(3,1fr)}.products-grid-5{grid-template-columns:repeat(3,1fr)}}
@media(max-width:900px){.products-grid,.products-grid-5,.products-grid-3{grid-template-columns:repeat(2,1fr)}.features-grid{grid-template-columns:repeat(2,1fr)}.footer-grid{grid-template-columns:1fr 1fr}}
@media(max-width:768px){nav{padding:0 16px}nav ul{display:none}.hero{padding:40px 16px}.cat-section{padding:48px 16px}.newsletter{flex-direction:column;padding:40px 16px}.newsletter-form{flex:1;width:100%}footer{padding:40px 16px 20px}.footer-grid{grid-template-columns:1fr}.section-tabs{padding:0 16px}}
